import WorkContent from "@/components/WorkContent";

export const metadata = {
  title: "Work Samples — Maximiliano Farias",
  description: "Bug reports, test cases, automation scripts and test execution reports.",
};

export default function WorkPage() {
  return <WorkContent />;
}
